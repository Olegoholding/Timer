<Window x:Class="Timer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Timer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="1000">
    <Window.Resources>

        <Style x:Key="CustomProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#feee77"/>
            <Setter Property="IsIndeterminate" Value="False"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#feee77"/>
            <Setter Property="Height" Value="24"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="Root">
                            <Border x:Name="PART_Track"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0"/>

                            <Border x:Name="PART_Indicator"
                                Background="{TemplateBinding Foreground}"
                                CornerRadius="0"
                                HorizontalAlignment="Left"
                                Margin="4">
                                <Grid x:Name="Animation" ClipToBounds="True">
                                    <Border x:Name="Indicator"
  
                                        Background="{TemplateBinding Foreground}"
                                        CornerRadius="0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsIndeterminate" Value="True">
                                <Setter TargetName="Indicator" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="#4c5844" CornerRadius="0" BorderThickness="1" BorderBrush="#858E7D">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4e5336"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#958831"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#3e4637"/>
            <Setter Property="RowBackground" Value="#3e4637"/>
            <Setter Property="AlternatingRowBackground" Value="#3e4637"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#858E7D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>

            <Setter Property="FontFamily" Value="Cascadia Code"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="RowHeight" Value="35"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

            <Style.Resources>
                <Style TargetType="DataGridRowHeader">
                    <Setter Property="Width" Value="0"/>
                </Style>

                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>

                    <Setter Property="FontFamily" Value="Cascadia Code"/>
                    <Setter Property="FontSize" Value="20"/>

                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#958831"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Trigger>

                        <!--<Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#4e5336"/>
                        </Trigger>-->
                    </Style.Triggers>
                </Style>

                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="#3e4637"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>

                    <Setter Property="FontFamily" Value="Cascadia Code"/>
                    <Setter Property="FontSize" Value="20"/>

                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>

                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderBrush" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>

                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#4c5844"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Height" Value="40"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="BorderBrush" Value="#858E7D"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Padding" Value="10,0"/>

                    <Setter Property="FontFamily" Value="Cascadia Code"/>
                    <Setter Property="FontSize" Value="20"/>
                    <Setter Property="FontWeight" Value="Regular"/>

                    <Setter Property="SeparatorBrush" Value="#858E7D"/>
                    <Setter Property="SeparatorVisibility" Value="Visible"/>
                </Style>

                <Style TargetType="ScrollBar">
                    <Setter Property="Background" Value="#3e4637"/>
                    <Setter Property="BorderBrush" Value="#858E7D"/>
                    <Setter Property="Width" Value="12"/>
                </Style>

                <Style TargetType="Thumb">
                    <Setter Property="Background" Value="#858E7D"/>
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Dust2.png" Stretch="UniformToFill"/>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Background="#4c5844" BorderBrush="#808A78" BorderThickness="1">
            <Border Margin="15" BorderBrush="#808A78" BorderThickness="1">
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <DataGrid IsReadOnly="True" x:Name="dataGrid" HorizontalScrollBarVisibility="Disabled"
                              
                              Style="{StaticResource CustomDataGridStyle}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="True"
                              CanUserDeleteRows="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Название" 
                                                Width="*" 
                                                Binding="{Binding Name}"/>

                            <DataGridTextColumn Header="Дата" 
                                                Width="150" 
                                                Binding="{Binding Date, StringFormat=dd.MM.yyyy}"/>
                        </DataGrid.Columns>

                    </DataGrid>
                    <Button Grid.Row="1" Content="Добавить" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Height="35" Width="100" FontFamily="Cascadia Code" FontSize="16" Foreground="White"/>
                </Grid>
            </Border>
        </Border>
        <GridSplitter Grid.Column="0" 
                      ShowsPreview="False" 
                      Width="3"     
                      HorizontalAlignment="Right" 
                      VerticalAlignment="Stretch" Background="#FF747D6C"
                      />
        <StackPanel Grid.Column="1" Margin="25" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Viewbox MaxHeight="400" >
                <Grid>
                    <Image Source="/bomb.png"/>
                    <TextBlock Foreground="Red" Margin="191,29,127,43" Width="220" FontSize="20" FontFamily="Cascadia Code" TextWrapping="WrapWithOverflow">
                        <Run Text="Дата детонации:" FontSize="16"/>
                        <LineBreak/>
                        <Run Name="date" Text="24.12.2025"/>
                        <LineBreak/>
                        <LineBreak/>
                        <Run Text="Осталось:" FontSize="16"/>
                        <LineBreak/>        
                        <Run Name="time" Text="1г. 0мес. 3д. 15мин. 10с." />
                    </TextBlock>
                </Grid>
            </Viewbox>
            <ProgressBar Name="progressBar" Style="{StaticResource CustomProgressBarStyle}" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Window>
